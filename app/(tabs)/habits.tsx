import { Repeat2 } from 'lucide-react-native';

import { PlaceholderScreen } from '@/components/feedback';

export default function HabitsScreen() {
  return (
    <PlaceholderScreen
      description="Habit schedules, logs, and streaks are intentionally not implemented yet."
      icon={Repeat2}
      title="Build consistency here"
    />
  );
}
